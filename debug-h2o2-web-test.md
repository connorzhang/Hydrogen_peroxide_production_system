# Debug Session: h2o2-web-test

Status: [CLOSED]

## Session

- Session ID: h2o2-web-test
- Goal: 分析最新 h2o2_final/AWL 程序与最新网页测试运行状态，启动测试环境并继续调试

## Hypotheses

1. 网页测试入口或启动脚本已变化，导致当前测试页面没有加载最新程序数据。
2. h2o2_final.awl 的关键指令或地址映射与网页解析器预期不一致，导致测试显示异常。
3. 最新网页测试依赖或构建产物未更新，导致运行的是旧版本页面或旧缓存。
4. 前端运行时存在接口、资源路径或模块加载错误，需要通过浏览器控制台和网络请求确认。
5. AWL 程序本身存在语法/结构段落差异，导致解析结果缺失或仿真状态不正确。

## Evidence Log

- Web 服务已用 `py .\plc_dashboard.py` 启动，监听 `http://127.0.0.1:5000/` 与 `http://192.168.0.244:5000/`。
- 浏览器可打开页面，标题为“双氧水生产 - PLC实时监测”，页面包含控制面板、参数设置、数字量输入/输出、模拟量、压力、液位、脉冲计数、瞬时流量、流程控制、内部标志位、泵输出等卡片。
- `/api/data` 持续返回 HTTP 200，前端无控制台报错。
- 当前 PLC 连接失败，接口返回 `connected=false`，错误为 `TCP connection failed: timed out`，页面状态为 `PLC断开: TCP connection failed: timed out`。
- 当前工作区未发现被 Git 跟踪文件删除记录，未发现独立命名为“测试面板”或 `panel` 的文件；最符合原网页测试面板的是 `plc_dashboard.py`。
- `py -m py_compile .\plc_dashboard.py` 通过，Python 语法正常。
- PLC 已恢复通信，`/api/data` 返回 `connected=true`，读取到液位、压力、流量、步骤、参数和泵输出数据：阳极液位约 `12.0cm`，阴极液位约 `12.1cm`，压力为 `0kPa`，步骤 `VB100=0/VB101=0`，PWM 参数 `8294`。

## Actions

- 已检查 `h2o2_final.awl` 主结构：OB1 初始化、HSC0/HSC1、模拟量、流量、顺控、急停均在同一文件中。
- 已启动 Flask 网页测试面板并通过浏览器访问验证。
- 已核对当前仓库删除状态，未发现原测试面板被 Git 标记删除。
- 已确认通信恢复后的实时接口数据正常返回，下一步可进行开机/关机/急停/参数设置等功能验证。
- 已按最新工艺调整 `h2o2_final.awl` 顺控：开机为补水升压、阳极先运行、阴极后运行、直流保持手动；关机为先停直流/制水/阴极，10 分钟后停阳极和空压机。
- 已同步网页步骤文案：`10=补水升压`，`20=阳极运行`，`30=阴阳极运行`，关机 `10=停止直流/阴极`，`20=阳极延时关机`。
- 已确认液位传感器量程为 2m，并将 `h2o2_final.awl` 中阳极/阴极液位换算从 0-100cm 改为 0-200cm。
- 已新增独立液位阀控制子程序 `SBR_LEVEL:SBR5`，OB1 在模拟量处理后调用；开机补水到 55cm，运行中阳极低于 45cm 补水、阴极低于 35cm 补水，达到 55cm 关闭，低于 20cm 置报警并关闭直流输出。
- 已从顺控子程序中移除补水阀直接开启逻辑，补水阀改由 `SBR_LEVEL` 独立管理；关机仍强制关闭补水阀、制水机、阴极、阳极和空压机。
- 已更新网页面板：液位参数改为“补水停止/开机液位”，范围扩展到 200cm，并新增液位控制阈值显示卡片。

## Stable Version Summary

- 当前稳定 PLC 程序为 `h2o2_final.awl`，当前稳定网页监控为 `plc_dashboard.py`。
- 压力换算按 4-20mA、0~600kPa；液位换算按 4-20mA、0~200cm。
- 补水逻辑已独立为 `SBR_LEVEL:SBR5`，仅在开机流程中执行，45cm 开阀、55cm 停阀，任一路补水时打开制水机 `Q1.0`。
- 低液位保护：阳极或阴极任一路低于 20cm，置位低液位报警并强制关闭直流仪输出 `Q1.2`。
- 阳极流量使用 `HC0` 差值计算，阴极流量因 `HC4` 现场不计数，当前使用 `I0.6` 的 `CTU C1` 差值计算。
- 流量系数已分路：阳极 `VD2016=221000.0`，阴极 `VD2028=88400.0`。
- 阳极泵默认 `VW2020=8294`，约 3.0V；阴极泵默认 `VW2026=5253`，约 1.9V。
- 阴极启动延时 `VW2024=100`，即 10s。
- 生产联锁：`VB100=30` 时，阳极流量 `VD1310 >= 0.5L/min`、阴极流量 `VD1330 >= 0.05L/min` 且无低液位报警，才允许打开 `Q1.2`。
- 生产缺流报警：生产阶段任一路流量不足时置 `V2303.5` 并关闭 `Q1.2`。
- 现场验证结果：两路流量正常后，`Q1.2` 输出正常，直流仪输出逻辑确认可用。

## Final Actions

- 页面已增加现场总览、纵向顺控流程、HSC1~HSC5 诊断、CTU 诊断、泵输出电压、阴极泵电压手动设置、流量达标和缺流报警显示。
- 已确认 `/api/data` 可实时读取 PLC 状态，并能显示 `VB100=30`、`V2303.3/V2303.4=ON`、`V2303.5=OFF`、`Q1.2=ON` 的生产状态。
- 当前调试会话关闭，后续按稳定版继续维护。
